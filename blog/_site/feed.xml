<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://0.0.0.0:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://0.0.0.0:4000/" rel="alternate" type="text/html" /><updated>2026-01-08T17:02:33+00:00</updated><id>http://0.0.0.0:4000/feed.xml</id><title type="html">Mundo Lunar</title><subtitle>Python, música e o que der na minha cabeça.</subtitle><author><name>Pierre Dechery</name></author><entry><title type="html">SQL Alchemy, Python e Bares</title><link href="http://0.0.0.0:4000/2025/06/01/sqlalchemy.html" rel="alternate" type="text/html" title="SQL Alchemy, Python e Bares" /><published>2025-06-01T00:00:00+00:00</published><updated>2025-06-01T00:00:00+00:00</updated><id>http://0.0.0.0:4000/2025/06/01/sqlalchemy</id><content type="html" xml:base="http://0.0.0.0:4000/2025/06/01/sqlalchemy.html"><![CDATA[<p>Resolvi finalmente estudar um pouco mais a fundo o <a href="https://www.sqlalchemy.org/">SQL Alchemy</a>, o famoso pacote do Python que possibilita a integração do Python com diversos Bancos de Dados relacionais.</p>

<p>Pensando em algo que pudesse tornar o estudo mais interessante, resolvi criar um app sobre um assunto bastante comum no cotidiano: contas de clientes em bares.</p>

<p>O que tornou tudo isso interessante pra mim foi descobrir como representar essas coisas num banco de dados utilizando a dobradinha Python/SQL Alchemy.</p>

<h2 id="bares-clientes-e-contas">Bares, Clientes e Contas</h2>

<p>Contas e Bares seria mais simples, um caso clássico de “Many To Many”: Clientes podem ter múltiplos Bares e Bares podem ter múltiplos Clientes.</p>

<p>Mas e as Contas?</p>

<p>Uma conta não é um atributo de um Cliente nem de um Bar, ela só existe quando há uma relação entre os dois.</p>

<p>O diagrama das tabelas desse Banco de dados portanto teria que ficar algo assim:</p>

<p><img src="/assets/img/erd.png" alt="Gráfico ERP" /></p>

<p>Como se vê, Clientes (CLIENT) e Bares (BAR) podem se associar múltiplas vezes e nos dois sentidos. As Contas por sua vez, precisariam ficar na tabela aonde a associação é feita, a Tabela Pivô (BILLS).</p>

<p>Felizmente, o SQL Alchemy <a href="https://docs.sqlalchemy.org/en/20/orm/basic_relationships.html#association-object">prevê esse caso</a> na documentação. Faltava então partir para a implementação.</p>

<h2 id="many-to-many-turbinado">Many To Many turbinado</h2>

<p>Quando trabalhamos com uma ORM é necessário criar classes que serão “mapeadas” com tabelas num banco de dados. Cada propriedade da classe será uma coluna da respectiva tabela.</p>

<p>Uma classe nesse contexto passa a ser chamada “modelo”.</p>

<p>Numa relação “Many To Many” tradicional, os modelos <code class="language-plaintext highlighter-rouge">Client</code> e <code class="language-plaintext highlighter-rouge">Bar</code> seriam suficientes. Mas aqui teremos que criar o também modelo <code class="language-plaintext highlighter-rouge">Bills</code>, pois o valor da conta ficará nele.</p>

<p>O código então ficará assim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>from sqlalchemy.orm import DeclarativeBase
from sqlalchemy import ForeignKey, String
from sqlalchemy.orm import Mapped, mapped_column, relationship

from typing import List

class Base(DeclarativeBase):
    pass

class Client(Base): 
    __tablename__ = "client"
    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(30), unique=True)
 
class Bar(Base):
    __tablename__ = "bar"
    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(60), unique=True)

class Bills(Base):
    __tablename__ = "bills"
    client_id: Mapped[int] = mapped_column(ForeignKey("client.id"), primary_key=True)
    bar_id: Mapped[int] = mapped_column(ForeignKey("bar.id"), primary_key=True)
    bill: Mapped[float]

</code></pre></div></div>
<p>Deixando de lado os elementos específicos do SQL Alchemy por enquanto, vemos que o código acima define três modelos, que são:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Client</code></li>
  <li><code class="language-plaintext highlighter-rouge">Bar</code></li>
  <li><code class="language-plaintext highlighter-rouge">Bills</code></li>
</ul>

<p>As propriedades de <code class="language-plaintext highlighter-rouge">Client</code> e <code class="language-plaintext highlighter-rouge">Bar</code> são simplemente o nome (<code class="language-plaintext highlighter-rouge">name</code>) e o <code class="language-plaintext highlighter-rouge">id</code>, que também é a chave primária. Já <code class="language-plaintext highlighter-rouge">Bills</code>, que será a Tabela Pivô, possui as chaves estrangeiras de <code class="language-plaintext highlighter-rouge">Client</code> e <code class="language-plaintext highlighter-rouge">Bar</code> e também a propriedade <code class="language-plaintext highlighter-rouge">bill</code>, que é justamente o valor da conta, como falamos acima.</p>

<h2 id="destrinchando-a-api-do-sql-alchemy">Destrinchando a API do SQL Alchemy</h2>

<p>O SQL Alchemy possui uma API extensa, onde há muitas formas de integrar o código Python com o Banco de Dados (até o uso da ORM é opcional). A opção adotada aqui é a considerada na documentação como a mais moderna, a “Declarative Mapping”.</p>

<p>Essa é a razão de usarmos a classe <code class="language-plaintext highlighter-rouge">DeclarativeBase</code> como classe-pai dos nossos modelos, como mostrado acima:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Base(DeclarativeBase):
    pass
</code></pre></div></div>

<p>Basicamente o “Declarative Mapping” prevê que para definir as colunas das nossas tabelas é necessário seguir o seguinte padrão:</p>

<p><code class="language-plaintext highlighter-rouge">{NOME DA COLUNA}: Mapped[{TIPO DA COLUNA}] = mapped_column({DEMAIS PROPRIEDADES DA COLUNA})</code></p>

<p>Este padrão espera que as informações sobre cada coluna de uma tabela sejam passadas em dois lugares distintos e complementares. No canto esquerdo, através do <em>annotation</em> <code class="language-plaintext highlighter-rouge">Mapped</code> e no canto direto através da função <code class="language-plaintext highlighter-rouge">mapped_column()</code>.</p>

<p>Na <a href="https://docs.sqlalchemy.org/en/20/orm/mapping_styles.html#orm-declarative-mapping">documentação oficial</a> da biblioteca você pode aprender mais sobre esta API.</p>

<blockquote>
  <p>O <em>annotation</em> <code class="language-plaintext highlighter-rouge">Mapped</code> pode despertar curiosidade naqueles que não estão tão habituados com o uso de tipos no Python. Para quem quiser se aprofundar um pouco mais no assunto, a documentação do SQL Alchemy <a href="https://docs.sqlalchemy.org/en/20/orm/mapping_styles.html#orm-mapping-styles">também oferece conteúdo detalhado</a>.</p>
</blockquote>

<p>O que nos interessa aqui, no entanto, é entender como o SQL Alchemy vai resolver os relacionamentos entre as entidades e como podemos trabalhar com elas no nosso código.</p>

<h2 id="populando-os-relacionamentos">Populando os relacionamentos</h2>

<p>Olhando o modelo <code class="language-plaintext highlighter-rouge">Bills</code>, podemos ver os relacionamentos entre bares e clientes declarados.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>client_id: Mapped[int] = mapped_column(ForeignKey("client.id"), primary_key=True)
bar_id: Mapped[int] = mapped_column(ForeignKey("bar.id"), primary_key=True)
</code></pre></div></div>

<p>Como num “Many To Many” tradicional, esta é a nossa Tabela Pivô e vai portanto levar as chaves estrangeiras (<code class="language-plaintext highlighter-rouge">ForeignKey</code>) de cada um.</p>

<p>O SQL Alchemy também oferece a função <code class="language-plaintext highlighter-rouge">relationship()</code>, que se destina a propriedades da classe que não vão ser mapeadas no banco de dados, mas ajudarão na hora de trabalhar com os relacionamentos no código.</p>

<p>Vamos ver como isso se dá na prática.</p>

<p>Primeiro vamos reescrever nossos modelos adicionando alguns relacionamentos.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Client(Base): 
    __tablename__ = "client"
    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(30), unique=True)
    bills: Mapped[List["Bills"]] = relationship(back_populates="client")
 
class Bar(Base):
    __tablename__ = "bar"
    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(60), unique=True)
    bills: Mapped[List["Bills"]] = relationship(back_populates="bar")

class Bills(Base):
    __tablename__ = "bills"
    client_id: Mapped[int] = mapped_column(ForeignKey("client.id"), primary_key=True)
    bar_id: Mapped[int] = mapped_column(ForeignKey("bar.id"), primary_key=True)
    bill: Mapped[float]
    bar: Mapped["Bar"] = relationship(back_populates="bills")
    client: Mapped["Client"] = relationship(back_populates="bills")
</code></pre></div></div>

<p>Adicionamos uma nova propriedade em <code class="language-plaintext highlighter-rouge">Client</code> e <code class="language-plaintext highlighter-rouge">Bar</code> chamada <code class="language-plaintext highlighter-rouge">bills</code>, que tem como valor o <code class="language-plaintext highlighter-rouge">relationship()</code>. Repare que em ambos o annotation <code class="language-plaintext highlighter-rouge">Mapped</code> declara o modelo com o qual estamos criando o relacionamento (<code class="language-plaintext highlighter-rouge">Bills</code>) - em ambos casos como lista (<code class="language-plaintext highlighter-rouge">Mapped[List["Bills"]]</code>). Isto já indica que o tipo de relação será “One To Many”.</p>

<p>Já em <code class="language-plaintext highlighter-rouge">Bills</code> incluímos duas propriedades, <code class="language-plaintext highlighter-rouge">bar</code> e <code class="language-plaintext highlighter-rouge">client</code>, que criam a relação com os modelos <code class="language-plaintext highlighter-rouge">Bar</code> e <code class="language-plaintext highlighter-rouge">Client</code>, respectivamente - mas aqui como unidade (<code class="language-plaintext highlighter-rouge">Mapped["Bar"]</code> e <code class="language-plaintext highlighter-rouge">Mapped["Client"]</code>).</p>

<p>Basicamente, essas novas propriedades nos permitirão trabalhar com os relacionamentos (criando, apagando etc) diretamente no código de modo muito simples.</p>

<h2 id="criando-e-relacionando-instâncias">Criando e relacionando instâncias</h2>

<p>Vamos partir então finalmente para um exemplo de criação de um Cliente que tenha uma Conta em um Bar, agora que temos todos os modelos - <code class="language-plaintext highlighter-rouge">Client</code>, <code class="language-plaintext highlighter-rouge">Bar</code> e <code class="language-plaintext highlighter-rouge">Bills</code> - devidamente criados e configurados.</p>

<blockquote>
  <p>Os exemplos em código dados aqui não necessitam de um banco de dados e podem ser testados com a ajuda do Python em seu terminal.</p>
</blockquote>

<p>Primeiro vamos criar um Cliente e uma Conta.</p>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> cliente <span class="o">=</span> Client<span class="o">(</span><span class="nv">name</span><span class="o">=</span><span class="s2">"Amigo do Zé"</span><span class="o">)</span>
<span class="gp">&gt;</span><span class="o">&gt;&gt;</span> conta <span class="o">=</span> Bills<span class="o">(</span><span class="nv">bill</span><span class="o">=</span>20.5<span class="o">)</span>
</code></pre></div></div>

<p>Agora vamos relacionar esta Conta ao cliente Criado.</p>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> cliente.bills.append<span class="o">(</span>conta<span class="o">)</span>
</code></pre></div></div>

<p>Usamos aqui a propriedade <code class="language-plaintext highlighter-rouge">bills</code> no modelo <code class="language-plaintext highlighter-rouge">Client</code>:</p>

<p><code class="language-plaintext highlighter-rouge">bills: Mapped[List["Bills"]] = relationship(back_populates="client")</code></p>

<p>Repare também que nesta propriedade temos o <em>annotation</em> <code class="language-plaintext highlighter-rouge">Mapped[List["Bills"]]</code>. Como falamos acima, isso significa que o Cliente possui uma <strong>lista</strong> de contas e por isso temos que usar o <em>append()</em> para inserir um novo relacionamento.</p>

<p>Agora nosso Cliente possui uma lista de contas que podemos inspecionar como quisermos. Por exemplo:</p>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> cliente.bills
<span class="go">[Bills(bill=20.5)]
</span></code></pre></div></div>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> cliente.bills[0]
<span class="go">Bills(bill=20.5)
</span></code></pre></div></div>

<p>ou</p>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> cliente.bills[0].bill
<span class="go">20.5
</span></code></pre></div></div>

<p>Vamos agora criar um Bar e adicioná-lo à Conta que acabamos de criar.</p>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> bar <span class="o">=</span> Bar<span class="o">(</span><span class="nv">name</span><span class="o">=</span><span class="s2">"Bar do Zé"</span><span class="o">)</span>
<span class="gp">&gt;</span><span class="o">&gt;&gt;</span> conta.bar <span class="o">=</span> bar
</code></pre></div></div>

<p>Repare que aqui não associamos o Bar diretamente ao Cliente, mas sim à Conta, que é a Tabela Pivô. Usamos a propriedade <code class="language-plaintext highlighter-rouge">bar</code> no modelo <code class="language-plaintext highlighter-rouge">Bills</code>, que cria um relacionamento com <code class="language-plaintext highlighter-rouge">Bar</code>.</p>

<p><code class="language-plaintext highlighter-rouge">bar: Mapped["Bar"] = relationship(back_populates="bills")</code></p>

<p>Aqui, como o relacionamento é “One to One” podemos simplesmente setar o valor da propriedade “bar”.</p>

<p>Agora vamos parar e pensar um pouco no que acabamos de fazer. Temos criadas uma instância de Cliente, uma de Conta e uma de Bar.</p>

<p>Em seguida relacionamos a Conta ao Cliente e em seguida um Bar àquela Conta.</p>

<p>Logo o Bar também está associado ao Cliente, não?</p>

<p>Clientes não possuem Bares, mas Contas sim. E podemos ver os Clientes e Bares de uma Conta.</p>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> conta.client
<span class="go">Client(id=None, name='Amigo do Zé')
</span></code></pre></div></div>

<p>O Cliente está lá!</p>

<p>Você pode estar se perguntando com a propriedade <code class="language-plaintext highlighter-rouge">client</code> foi atualizada sem que fizéssemos nada. A resposta é o <code class="language-plaintext highlighter-rouge">back_populates</code>:</p>

<p><code class="language-plaintext highlighter-rouge">bills: Mapped[List["Bills"]] = relationship(back_populates="client")</code></p>

<p>Quando relacionamentos uma Conta a um Cliente o <code class="language-plaintext highlighter-rouge">back_populates</code> atualiza a propriedade <code class="language-plaintext highlighter-rouge">client</code> em <code class="language-plaintext highlighter-rouge">Bills</code>.</p>

<p>Da mesma forma, quando adicionamos um Bar a uma Conta usamos a propriedade <code class="language-plaintext highlighter-rouge">bar</code> em <code class="language-plaintext highlighter-rouge">Bills</code>:</p>

<p><code class="language-plaintext highlighter-rouge">bar: Mapped["Bar"] = relationship(back_populates="bills")</code></p>

<p>Aqui, o <code class="language-plaintext highlighter-rouge">back_populates</code> atualiza a propriedade <code class="language-plaintext highlighter-rouge">bills</code> em <code class="language-plaintext highlighter-rouge">Bar</code>. Vamos testar:</p>

<div class="language-shell-session highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">&gt;</span><span class="o">&gt;&gt;</span> bar.bills
<span class="go">[Bills(bill=20.5)]
</span></code></pre></div></div>

<p>Aí está! Legal, não é?</p>

<p>Se está parecendo um pouco confuso à primeira vista, recomendo abrir um terminal do Python, importar os Modelos criados aqui e testar todas as propriedades.</p>

<p>https://www.reddit.com/r/webdev/comments/1bsi9eq/best_place_for_devs_to_post_blogs/</p>]]></content><author><name>Pierre</name></author><summary type="html"><![CDATA[Resolvi finalmente estudar um pouco mais a fundo o SQL Alchemy, o famoso pacote do Python que possibilita a integração do Python com diversos Bancos de Dados relacionais.]]></summary></entry></feed>